{
  "builds": [
    {
      "src": "./package.json",
      "use": "@vercel/next",
      "config": {
        "buildCommand": "./vercel-build.sh",
        "outputDirectory": "packages/dashboard/.next"
      }
    }
  ]
}
