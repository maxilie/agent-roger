{
  "builds": [
    {
      "src": "./package.json",
      "use": "@vercel/next",
      "config": {
        "buildCommand": "yarn run build:dashboard-prod",
        "outputDirectory": "packages/dashboard/.next"
      }
    }
  ]
}
