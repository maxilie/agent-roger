export type RogerTask = {
  id: number;
  type: string;
};

const handleTask = async (task: RogerTask): Promise<void> => {
  // TODO get task handler function
  // TODO handle next step in task logic
};

export { handleTask };
